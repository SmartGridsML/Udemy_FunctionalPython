# tuple example my_Tuple = (1 ,2, 3) ; comma indicates tuple data structure
# single tuple (1,)

# Packed values refers to values that are bundled together in some way
# eg Tuples, list, string, sets, dictionaries
# iterable = packed value
# UNPACKING = splitting packed values into individual variables contained in list/tuple etc

a , b = 10 , 20
print(a, b)
a , b = b, a
print("hello")
